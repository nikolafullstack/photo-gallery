export const selectLoading = (state) => state.flow.loading;
export const selectError = (state) => state.flow.error;
export const selectFlow = (state) => state.flow.flow;
export const selectPage = (state) => state.flow.page;
export const selectItemsPerPage = (state) => state.flow.itemsPerPage;
export const selectCategory = (state) => state.flow.category;
